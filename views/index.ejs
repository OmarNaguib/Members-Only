<h2>
  <% if (currentUser) { %>
  Current User:
  <%= currentUser.fullName %>
  <a href="/log-out">log out</a>
  <% } else {%>
  <a href="/log-in">log in</a> <a href="/sign-up">Sign up</a>
  <%}%>
</h2>

<h1><%= title %></h1>
<p>
  Welcome to
  <%= title %>
</p>
<a href=""></a>
